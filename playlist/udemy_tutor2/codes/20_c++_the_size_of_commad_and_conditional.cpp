#include <iostream>
using namespace std;

// (exp 1) ? exp 2(if exp 1 is true) : exp 3(if exp 1 is false)

int main()
{
    // Part 1
    // int res;
    // res = (6 <= 6) || (5 < 3);
    // cout << res << endl;
    // res = (6 <= 6) && (5 < 3);
    // cout << res << endl;
    // res = !(6 <= 6);
    // cout << res << endl;

    // Part 2
    double d = 5.0;
    cout << sizeof(d) << endl;
    cout << sizeof(',') << endl;
    return 0;
}