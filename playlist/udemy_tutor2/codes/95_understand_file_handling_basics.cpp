#include <iostream>
using namespace std;

int main()
{
    /*
    void open() (const char* file_name, ios::openmode mode);
    fstream new file;
    new file.open("newfile.txt", ios::out)
    new file.open("newfile.txt", ios::in)
    new file.open("newfile.txt", ios::out | ios::app)
    */

    return 0;
}